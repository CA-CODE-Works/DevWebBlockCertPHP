# DevWebBlockCertPHP

## Make the following changes to the core/init.php file

**On line 19, Add your BlockCypher Token**

$token = '\<BlockCypher Token\>';

**On line 22, Enter the path to where you want the CSV written to. Default is in the core directory**

$dir = __DIR__ . '/roster.csv';
